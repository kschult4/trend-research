# n8n Credentials Reference - Extracted from Working Workflows

Based on analysis of your working n8n workflows (v1 and v2), here are the credentials already configured in your n8n instance:

---

## SSH Credentials (Container 118)

**Used for:** Running commands on Container 118 (CrewAI/Catalyst execution)

```json
"credentials": {
  "sshPrivateKey": {
    "id": "1",
    "name": "Container 118 SSH"
  }
}
```

**Configuration:**
- Type: SSH (Private Key)
- Name: `Container 118 SSH`
- ID: `1`
- Host: `192.168.1.18`
- Port: `22`
- Username: `root`
- Private Key: [Your SSH private key]

**Used in workflows:**
- n8n-strategic-intelligence-daily.json (v1)
- n8n-strategic-intelligence-daily-v2.json (v2)
- **Should be used in:** Phase 3 button handler workflow

---

## Slack API Credentials

**Used for:** Posting messages to Slack, updating messages via response_url

```json
"credentials": {
  "httpHeaderAuth": {
    "id": "1",
    "name": "Slack Trend Reporting Token"
  }
}
```

**Configuration:**
- Type: HTTP Header Auth
- Name: `Slack Trend Reporting Token`
- ID: `1`
- Header Name: `Authorization`
- Value: `Bearer xoxb-YOUR-SLACK-BOT-TOKEN`

**How it's referenced in HTTP Request nodes:**
```json
"headerParameters": {
  "parameters": [
    {
      "name": "Authorization",
      "value": "Bearer {{ $credentials.slackToken }}"
    }
  ]
}
```

**Used in workflows:**
- n8n-strategic-intelligence-daily.json (v1)
- n8n-strategic-intelligence-daily-v2.json (v2)
- **NOT needed in Phase 3** (uses response_url directly, no auth required)

---

## Phase 3 Workflow Credential Requirements

The Phase 3 button handler workflow needs:

### 1. SSH Credentials (Required)

Used by these nodes:
- **Run Catalyst** - SSH to Container 118 to execute Catalyst
- **Log Ignore Action** - SSH to Container 118 to append log file

**How to configure:**
```json
"credentials": {
  "sshPrivateKey": {
    "id": "1",
    "name": "Container 118 SSH"
  }
}
```

### 2. Slack Credentials (NOT Required)

The Phase 3 workflow uses Slack's `response_url` for updating messages, which doesn't require authentication. The `response_url` is:
- Provided by Slack in the button interaction payload
- Valid for 30 minutes
- Pre-authenticated by Slack

**No Slack credentials needed for:**
- Acknowledge - Processing node
- Update Slack - Develop Success node
- Update Slack - Ignored node

---

## Credential Mapping Summary

| Node Name | Credential Type | Credential Name | ID | Required? |
|-----------|----------------|-----------------|-----|-----------|
| Run Catalyst | SSH Private Key | Container 118 SSH | 1 | Yes |
| Log Ignore Action | SSH Private Key | Container 118 SSH | 1 | Yes |
| Acknowledge - Processing | None | N/A | N/A | No (uses response_url) |
| Update Slack - Develop Success | None | N/A | N/A | No (uses response_url) |
| Update Slack - Ignored | None | N/A | N/A | No (uses response_url) |

---

## How to Verify Credentials in n8n UI

1. Go to n8n: https://n8n.kyle-schultz.com
2. Click **Settings** (gear icon) → **Credentials**
3. You should see:
   - **Container 118 SSH** (Type: SSH Private Key)
   - **Slack Trend Reporting Token** (Type: HTTP Header Auth)

If "Container 118 SSH" is missing:
1. Click **Add Credential**
2. Select **SSH (Private Key)**
3. Name: `Container 118 SSH`
4. Host: `192.168.1.18`
5. Port: `22`
6. Username: `root`
7. Private Key: Paste your SSH private key for Container 118
8. Click **Save**

---

## Updated Phase 3 Workflow

I've created `n8n-slack-button-handler-phase3-with-credentials.json` which has:
- SSH credentials pre-populated with ID: 1, Name: "Container 118 SSH"
- Matches your existing working workflows
- No Slack credentials required (uses response_url)

Import this version and n8n will automatically link to your existing "Container 118 SSH" credential.

---

*Generated by analyzing working n8n workflows v1 and v2*
